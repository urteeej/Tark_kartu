import 'package:flutter/material.dart';

class Nustatymai extends StatelessWidget {
  const Nustatymai({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Color(0xFFF3BF82),
    );
  }
}

